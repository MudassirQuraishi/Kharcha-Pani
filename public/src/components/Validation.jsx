
// import Styles from './Validation.modules.css'
const Validation = ({ data }) => {
    return (
        <div className='validation-container'>
            <p className='validation'>{data}</p>
        </div>
    );
};

export default Validation;
